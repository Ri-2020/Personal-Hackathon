import 'package:flutter/material.dart';

//FUNCTIONS
EdgeInsetsGeometry paddingAll(double a) {
  return EdgeInsets.all(a);
}

EdgeInsetsGeometry marginAll(double a) {
  return EdgeInsets.all(a);
}

// CONSTANTS
MainAxisAlignment spaceBetween = MainAxisAlignment.spaceBetween;
const fontOne = 15.0;
